import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  gridItem: {
    marginTop: '1rem',
    position: 'relative',
    padding: 0,
  },
  reviews: {
    padding: '1rem',
    margin: '-1rem 0 0rem',
  },
}));
